Features

Current:
* urequire config
  - justCopy : false #
     # can be []<String> of filenames / templates to skip uRequiring.

  - 'requirejs config / requirejs 'link'
    Remove hardcoded paths!

Future:
* watch - copy coffeescript's method ?
* use Esprima instead of uglify

* check jam, package.json, requirejs.packages

General:
  * more tests
  * more docs


