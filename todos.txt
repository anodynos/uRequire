Features

* r.js optimize & bundle

* watch - copy coffeescript's

* use Esprima instead of uglify

* function sniff it!

* urequire config
  - uRequire : [ '*.js'] #default, if ommited - can include list of inclusions
  - justCopy : false #
                     # can be []<String> of filenames / templates to skip uRequiring.
  - ignore: files to do nothing with - eg "myNotes.txt" !

  - a 'requirejs: config: or requirejs 'link'!

  - check jam, package.json, requirejs.packages

General:
  * more tests
  * more docs


